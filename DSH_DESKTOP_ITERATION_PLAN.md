# DSH Desktop 桌面版迭代计划

> 基线日期：2026-08-22
> 产品对标：**Claude Code**  
> 技术底座：**DeepSeek Harness**  
> 首发平台：Windows 10/11 x64  
> 默认节奏：每个计划迭代形成一个可运行 Latest Build；验证通过后直接覆盖电脑中已安装的旧版。Stable 不按日程自动晋升，只有用户明确下达“更新 Stable”命令后才更新。
> 执行状态：2026-08-24 V0.5.4 已完成真实会话分支、最终覆盖、数据保留和 GitHub 发布完整性验证，并固定为当前 Stable；下一版 V0.5.5 进入 Latest 通道和权限中心。
> 产品对标证据：[Claude Code 产品能力基线](CLAUDE_CODE_PRODUCT_BENCHMARK.md)  
> 工程实现参考：[DeepSeek Harness 同源桌面项目](GITHUB_DEEPSEEK_HARNESS_DESKTOP_REFERENCE.md)

## 1. 产品定义

### 1.1 产品定位

DSH Desktop 是一个基于 DeepSeek Harness 的本地 Agent 编程桌面工作台，产品体验对标 Claude Code。

它让开发者在一个窗口内完成代码理解、任务规划、工具执行、文件修改、命令和测试、Diff 审查、回退恢复与并行开发。

它不是普通聊天客户端，也不是 ERP 或其他行业专用产品。

### 1.2 核心用户

- 在 Windows 本地代码仓库中工作的开发者。
- 希望以可视化方式使用 Agent 编程能力的用户。
- 需要同时监督多个 Agent 任务、文件变化和测试结果的技术团队成员。

### 1.3 唯一主流程

`打开本地仓库 → 新建/恢复会话 → 理解或规划 → 用户确认权限 → Agent 修改代码并运行命令 → 审查 Diff 与测试 → 接受、拒绝或回退 → 继续/分支/并行处理`

任何 1.0 之前的新增功能必须直接服务这条主流程。

### 1.4 发布通道

- **Latest**：按本计划继续迭代的最新可运行版本。每版完成验证后发布为 GitHub Pre-release，并直接覆盖安装到用户当前电脑；它可以持续前进，但不自动改变 Stable。
- **Stable**：面向普通用户的稳定基线。当前固定为 V0.5.4；只有用户明确下达“更新 Stable”命令，且指定 Latest 版本通过稳定版验收后，才取消其预发布状态并晋升 Stable。
- **回退**：Latest 出现回归时，保留 Stable 安装包、标签和数据兼容路径；不得为追求版本号删除或覆盖 Stable 发布物。
- GitHub 内置的 `Latest release` 标签继续指向 Stable；产品文档中的 Latest 指开发最新通道，对应 GitHub Pre-release，避免普通用户误装测试版本。
- **独立品牌门禁**：V0.5.4 Stable 保持现名；后续 Latest 在扩大公开推广前完成唯一产品名、`productName`、`appId`、安装目录、快捷方式和用户数据迁移设计，避免与 `dataelement/dsh-desktop` 同名冲突。具体名称必须由用户确认后再实施。

## 2. 产品与技术边界

| 层级 | 负责范围 | 不负责 |
|---|---|---|
| DeepSeek Harness | Agent loop、会话、模型、工具、权限、Goals、Subagents、Skills、Plugins、Hooks、MCP、Web UI | Windows 安装、托盘、桌面升级和原生窗口 |
| DSH Desktop 宿主 | Harness 进程、窗口、单实例、托盘、通知、原生目录选择、日志、更新、诊断、安装包 | 重新实现 Agent loop、模型协议和插件框架 |
| DSH 开发者工作台 | 项目/会话导航、Plan、工具过程、Diff、文件、终端、预览、任务和恢复界面 | 行业化 ERP、BI 或办公交付流程 |

### 2.1 强制原则

1. 固定 DeepSeek Harness tag、commit、package version 和 Node 版本，不隐式使用 latest。
2. Harness 只监听随机 `127.0.0.1` 端口。
3. 用户数据、会话、配置和插件位于安装目录之外。
4. Renderer 保持 sandbox、`contextIsolation` 和无 Node integration。
5. 复用 Harness 会话、工具、权限和扩展体系，不建立平行实现。
6. 桌面增强优先通过 Harness Plugin、Client Slot 和窄原生接口实现。
7. 每个 Latest Build 在测试和 packaged smoke 通过后，先快照已安装程序与用户数据，再直接覆盖旧版；启动验证失败则恢复 last-known-good。
8. DSH Desktop 以软件模型页托管的 Key 为最高优先级；Windows 环境变量不传入 Harness 子进程，普通备份不复制凭据文件。

## 3. 对标 Claude Code 的产品能力

### 3.1 1.0 核心对标

- 本地仓库与持久会话。
- Plan 模式和受控执行模式。
- Agent 工具过程实时可见。
- 执行中停止、纠正和继续。
- 文件 Diff、接受、拒绝和回退。
- 集成终端、文件查看和应用预览。
- 自动 Checkpoint、Rewind、会话继续和分支。
- 并行会话、Git worktree 和 Subagents。
- Skills、Plugins、Hooks 和 MCP 管理。
- 项目规则、记忆和上下文可见。

### 3.2 1.0 之后再对标

- SSH、远程和云端执行环境。
- PR 自动修复、自动合并和复杂托管。
- 定时任务与长期后台自动化。
- Computer Use。
- 手机派发与跨设备继续。
- 企业 SSO、集中策略和组织权限。

## 4. 桌面工作区结构

```text
┌────────────────────────────────────────────────────────────┐
│ 项目 / 分支 / 模型 / 权限模式 / 会话状态 / 全局命令       │
├──────────────┬──────────────────────────┬──────────────────┤
│ 项目与会话   │ Chat / Plan / 权限交互   │ Diff / File      │
│ 并行任务     │ Agent 工具过程           │ Preview / Tasks   │
│ Worktrees    │ 用户输入                 │ Subagents         │
├──────────────┴──────────────────────────┴──────────────────┤
│ Terminal / Test / Logs                                     │
└────────────────────────────────────────────────────────────┘
```

- 左侧：项目、会话、并行任务和 Worktree。
- 中间：Chat、Plan、工具过程、权限确认和输入区。
- 右侧：Diff、文件、预览、任务与 Subagent，可按需关闭。
- 底部：终端、测试和日志，可调整高度。
- 面板可调整、移动、关闭和恢复；布局按项目保存。
- Windows 使用系统窗口规范，不复制 macOS 窗口控件。
- 内容面保持稳定实底；材质只用于工具栏、侧栏和临时浮层。

### 4.1 Agent 状态

统一使用：

- 排队中。
- 运行中。
- 等待用户确认。
- 已完成。
- 已失败。
- 已取消。
- 已断开。

状态必须同时使用文字和图标，不能只依赖颜色。

## 5. 当前基线重新判定

### V0.4.5 Daily Build：HTML 与本机开发服务器预览已完成

- 在现有 Harness 工作台中增加集成应用预览，不建立新窗口、Agent、会话或平行项目入口。
- HTML Quick Look 可直接启动当前工作区预览；软件绑定随机 `127.0.0.1` 端口并提供 HTML、CSS、JavaScript、图片、字体、JSON、SVG 和 WASM 相对资源。
- 可连接既有 `127.0.0.1`、`localhost` 或 `::1` HTTP/HTTPS 开发服务器，支持端口简写、重新加载、浏览器打开和离线监控。
- 软件管理的预览在停止、关闭面板、切换仓库或退出时释放端口；外部开发服务器只监控、不结束。
- 预览拒绝远程地址、凭据、Harness 自身 origin、越界路径、链接/目录联接、疑似凭据文件和超过 32 MiB 的托管文件；iframe 使用独立 loopback origin 与 sandbox。
- 面板显示未启动、连接中、可用、离线、失败和已停止状态，并区分软件管理端口与外部本机服务。
- 85 项自动化回归通过；真实桌面检查修复了 Electron 35 子框架导航事件签名兼容问题，`index.html` 已在普通和最大化窗口中实际渲染，停止后端口不可访问。
- V0.4.5 已直接覆盖；Windows 注册版本正确，13 个持久会话、软件 Key、桌面状态和工作台状态均保留。

### V0.4.4 Daily Build：持久交互式 PTY 已完成

- 将 V0.4.1 的单命令运行器替换为一个持续存在的 PowerShell PTY，同一 Shell 内可连续执行命令并保留工作目录、变量、交互提示和 ANSI 输出。
- Electron 主进程持有终端状态；Harness 页面重载后恢复运行状态、工作目录、尺寸和最近 200,000 个字符输出，不重启 Shell。
- 使用独立的内置 Node 宿主加载固定 `node-pty 1.2.0-beta.15`，页面由 `@xterm/xterm 6.0.0` 与 `@xterm/addon-fit 0.11.0` 渲染；用户无需安装 Node.js 或 pnpm。
- 启动前进行一次 Windows 原生风险确认；输入、协议和恢复输出均有界，OSC 52 剪贴板写入序列被过滤。
- 软件托管 `DEEPSEEK_API_KEY` 不进入 PTY 宿主或 PowerShell 环境；终端运行期间 Git 接受/拒绝禁用，停止后重新建立用户修改保护基线。
- 停止操作结束完整 Windows 进程树；已使用真实长运行子进程验证，不只验证主进程退出。
- 78 项自动化回归通过；真实 Windows PTY 连续命令、尺寸变化、恢复、Key 隔离、失败路径和进程树停止均通过。
- 正常与最大化布局完成桌面视觉检查；终端、Harness 和左右工作台未裁切，可访问性树包含状态、路径、输入和控制按钮。
- V0.4.4 已直接覆盖；Windows 注册版本正确，13 个持久会话、软件 Key、桌面状态和工作台状态均保留。

### V0.4.3 Daily Build：中文过程与界面标签一致性已完成

- Harness 固定运行时从 `@deepseek-ai/dsh@0.1.0-rc.8` 升级为官方 `0.1.1-rc.2`，带入视觉模型、图像上传复用/预处理、多行提问输入与上游安全修复。
- 通过 Harness 官方 `--patch` 覆盖部署 persona：中文提问默认让可见思考、工具调用说明、计划、进度、问题和结论全部使用简体中文。
- 语言规则明确保留代码、命令、路径、标识符、API 名称、错误原文和原始工具输出，不为汉化改写可执行内容。
- 在宿主页面注入有界 MutationObserver，仅翻译上游遗留的 `Session log`、`Think`、`Thinking` 和空输出占位；跳过代码块、输入框和可编辑区域。
- 已有会话中已经生成的英文过程保留原文；新回合从提示词层按用户语言生成，避免篡改持久会话记录。
- 中文补丁作为独立 `extraResources` 文件进入安装目录；缺失时 Harness 启动失败并给出中文修复提示，不静默退回英文行为。
- 插件边界已按源码、依赖闭包和默认 Web 配置三层核验：打包 188 个 `dsh`/`dsh-*` 包、默认组合 135 个插件行；社区插件与非默认官方替代包不静默内置。
- 74 项自动化回归通过；配置合成可见完整语言规则，解包版与正式安装版 Harness 均返回 HTTP 200。
- V0.4.3 已直接覆盖；注册版本、安装哈希、中文资源和软件 Key 正常，原 12 个持久会话保留并在真实模型验证后新增第 13 个会话。

### V0.4.2 Daily Build：桌面工作台文件切片已完成

- 正式入口已跟随升级为官方 `@deepseek-ai/dsh@0.1.1-rc.2`，不再直接调用 Chat Completions。
- 内置并校验 Node v24.19.0，目标电脑不需要预装 Node。
- `HarnessSupervisor` 已实现随机回环端口、就绪、日志、停止、重试和错误状态。
- Harness Profile、会话和配置保存在安装目录之外。
- 开发态和最终 `win-unpacked` 均完成真实 Harness HTTP 200 smoke。
- 实际 Electron 窗口已进入官方 Harness 首启界面并完成视觉检查。
- Windows 原生目录选择、最近仓库、仓库状态持久化和 Harness 工作目录切换已完成。
- Windows 原生会话菜单已接入新建、搜索、定位列表、持久会话计数和数据目录；会话正文仍由 Harness 管理。
- Windows 原生模型菜单已接入官方模型设置与安全 Key 诊断；诊断只报告状态，不暴露密钥。
- DSH Desktop 已在 Harness 子进程边界隔离 Windows `DEEPSEEK_API_KEY`，软件模型页托管凭据成为最高优先级来源。
- 首次 Key 引导和模型设置输入框已解除环境变量只读锁；软件 Key 状态与优先级已在原生菜单和状态页可见。
- 普通 Daily Build 回滚备份已明确排除 `.credentials.yaml`，避免凭据被复制到工作区备份。
- 软件中已保存的 Key 经连续覆盖安装后仍可用，并已完成真实模型正常结束、长输出和真实工具调用验证。
- Windows 原生 Agent 菜单已显示空闲、运行中、等待确认和不可用状态，并提供停止、聚焦补充输入、排队插话和定位待确认操作。
- 真实长输出中已验证排队纠正消息进入会话，以及从桌面菜单中断生成；停止后回到空闲且无误报弹窗。
- Agent 状态与动作采用固定双语 UI 标签和白名单，点击时状态刚好变化的竞态会刷新收敛。
- Windows 原生工具菜单已显示最近工具状态、工具类别、测试结果和当前窗口调用/失败/停止计数，并提供工具定位与轨迹入口。
- 已从官方 rc.8 对话/轨迹 DOM 提取真实工具生命周期和测试退出码；长轨迹采用有界首尾摘要，避免工具命令或尾部退出码被截断。
- 使用软件 Key 的真实 `pwsh` 调用验证了失败路径：rc.8 Workspace Write 工具进程以 `3221225477 (0xC0000005)` 退出且无输出；桌面正确显示失败，但不宣称测试成功。
- V0.3.5 显示当前权限模式和 PowerShell 兼容状态，并提供官方权限入口与明确风险说明；不会自动切换权限。
- V0.3.6 新增原生“变更”菜单，可定位官方 Harness 最近 Diff 或 Produced 文件入口。
- 最近产物文件可“接受并暂存”；已跟踪文件可恢复到当前 Git 暂存版本，新文件拒绝时进入 Windows 回收站。
- Agent 运行/等待确认时禁止接受或拒绝，非 Git 仓库仅允许查看。
- 打开仓库和进入新 Agent 回合时建立原有修改保护基线；命中已有未暂存/未跟踪内容时禁止一键拒绝。
- V0.3.7 通过官方 Workspace/Session RPC 将桌面仓库注册为同路径 Workspace，并显式注入 `DSH_CWD`。
- 目标 Workspace 的未归档空白会话会被复用；没有时创建新会话，并让官方 UI 稳定切换到目标会话。
- 已修复已有用户数据下会话列表初始化导致的持久化竞态；原生“项目”菜单可显示实际 Harness 同步目标。
- 48 项自动化回归已覆盖真实临时 Git 仓库中的暂存、恢复、新文件、保护路径，以及 Workspace/Session 映射与启动稳定性。
- 使用软件 Key 完成两轮真实 Read/Edit；接受后暂存 `alpha=2`，后续编辑为 `alpha=3` 再拒绝恢复到 `alpha=2`，已有暂存内容保留。
- 隔离 Full Access 会话已用软件 Key 完成真实文件读取、定点修改和 `pwsh` 测试；测试 1/1 通过、退出码 0，并经独立复核。
- rc.8 官方 UI 的会话继续、搜索、重命名、归档和分支能力已经核对并完成入口验证，不重复实现。
- 状态页可同时显示 Harness 运行、工作区、已保存会话和模型配置状态。
- Harness 已改为无目录联接的平铺生产闭包，解决 V0.2.0 的 NSIS/pnpm 安装兼容问题。
- V0.3.7 已从 V0.3.6 直接覆盖到正式目录；安装版 Harness 返回 HTTP 200，Workspace 同步、注册信息、软件 Key 和 8 个持久会话正常。
- V0.3.8 原生 Agent 菜单已接入官方 Plan 状态、进入、定位确认与退出；`/plan` 的 rc.8 两次 Enter 提交流程已固定并回归。
- 原生“变更”菜单已扩展为多文件列表和状态统计，支持逐文件与全量预检后的批量接受/拒绝。
- 多文件拒绝继续保护原有用户修改、Git 暂存基线和新文件回收站语义；存在受保护或不可用文件时整批拒绝。
- 使用软件 Key 完成真实三文件 Plan，官方 `exit_plan_mode` 确认后才执行；批量接受与第二轮批量拒绝均通过。
- 50 项自动化回归通过；V0.3.8 已覆盖安装，正式版 HTTP 200、注册版本、软件 Key、9 个会话和安装哈希正常。
- V0.4.0 在官方 Harness 页面右侧增加常驻 Git 变更审查面板，可同时显示待审、受保护和已接受文件。
- V0.4.1 在 Harness 下方增加同工作区 PowerShell 单命令面板，每条命令原生确认，支持有界输出和完整进程树停止。
- 软件托管 `DEEPSEEK_API_KEY` 不进入终端子进程；命令结束后刷新 Git 保护基线，用户终端修改不会被当成可一键拒绝的 Agent 修改。
- 终端支持 160–420px 高度、快捷键、开关与高度持久化；当前明确不是持久交互式 PTY。
- 面板展示有界真实工作区/暂存区 Diff；未跟踪文本和二进制文件使用大小与类型保护，不读取无界内容。
- 面板支持 280–520px 拖动、键盘调节、隐藏、恢复、聚焦和宽度重置；独立工作台状态跨 Harness 重载与应用重启保留。
- 逐文件和批量操作继续经过既有原生确认、路径验证、保护基线和回收站语义，不在 Renderer 内直接执行 Git。
- 隔离真实仓库已验证待审/保护/接受状态、取消不变、确认单文件暂存和暂存 Diff；正常与最大化视觉通过。
- 63 项自动化回归通过；V0.4.1 已从 V0.4.0 直接覆盖，正式版 HTTP 200、注册版本、软件 Key 和 9 个会话正常。
- V0.4.2 在 Harness 左侧增加同 Workspace 懒加载文件树，目录按需读取并提供有界文件名搜索。
- 安全文本使用只读 Quick Look 浮层查看；路径、编码、行数和大小可见，`Esc` 关闭后恢复文件焦点。
- 右侧 Diff 的“查看文件”已打通真实定位，可清空搜索、展开父目录、选中准确路径并打开预览。
- 文件服务拒绝绝对/越界路径、父目录跳转、链接和目录联接；凭据、私钥、二进制、大文件与不支持编码默认不预览。
- 文件面板支持 220–380px、快捷键和状态持久化；工作台状态升级到 schema v3 并保留原审查/终端布局。
- 70 项自动化回归通过；V0.4.2 已从 V0.4.1 直接覆盖，正式版 HTTP 200、注册版本、软件 Key 和 10 个会话正常。

### V0.4.6 后的已知缺口

- 真实模型会话已通过；会话继续当前复用 Harness UI，尚未形成独立桌面恢复/继续状态视图。
- 会话继续、重命名、归档和分支目前复用官方 Harness UI；桌面原生入口已覆盖高频新建、搜索和定位操作。
- 工具失败与隔离成功读改测路径已可见；桌面仓库、rc.8 Workspace、目标会话和多文件逐项/批量接受拒绝已形成真实闭环。
- 常驻 Git Diff、只读文件树/文本/图片/PDF 查看、一个持久 PTY 和 HTML/本机服务器应用预览已完成；尚缺代码编辑器、终端标签/分屏、Checkpoint 和会话级 Rewind。
- 真实桌面验证发现仓库中的 `.png` 实为 JPEG，因此专用预览改为先验证实际内容：受支持图片之间的扩展名误写允许打开并明确提示，图片/PDF 跨类型伪装仍阻止。
- Windows 安装包未签名；V0.3.0 → V0.3.1 纯平铺结构直接覆盖已实测 304.21 秒。
- A/B/C ERP 概念稿退出产品主线，只作为归档视觉探索。
- 历史会话中已写入的英文思考与工具说明不会自动翻译；V0.4.3 只修正固定标签并约束后续模型生成内容。

因此 V0.4.6 已完成文本、图片、PDF、HTML 和本机开发服务器的第一轮只读查看闭环。下一切片进入命令面板和快捷键帮助；版本号断言将集中管理，避免后续 Daily Build 因测试写死旧版本遗漏。

## 6. 版本能力规划

| 版本 | 周期 | 阶段目标 | 用户可用能力 | 发布门槛 |
|---|---:|---|---|---|
| P0 / V0.1.1 | 已完成 | 视觉与安装原型 | 基础窗口、主题、双语、安装包 | 不作为 Harness 和 Agent 功能基线 |
| V0.2 | 第 1 天，已完成 Daily Build | 接入 Harness 底座 | 无需安装 Node；启动官方 Harness；Profile、会话、模型、权限、工具和插件底座可用 | 最终解包版真正拉起 Harness；HTTP 200；退出无对应子进程残留 |
| V0.3 | 第 2–9 天，已完成 Daily Build | 建立 Agent 编程 MVP | 打开/记忆仓库、Workspace/会话同路径映射、软件 Key 最高优先级、真实模型连接、Stop & steer、工具生命周期、失败诊断、官方 Plan 门禁，以及真实多文件逐项/批量接受拒绝 | 已完成“仓库—Plan—确认—修改—测试/工具证据—多文件 Diff—确认”主闭环 |
| V0.4 | 第 2 周，进行中 Daily Build | 形成桌面开发工作台 | V0.4.0 常驻 Git Diff；V0.4.1 同工作区受控 PowerShell；V0.4.2 文件树与只读查看；V0.4.3 中文过程；V0.4.4 持久 PTY；V0.4.5 HTML/本机服务器预览；V0.4.6 图片/PDF；V0.4.7 命令面板 | 关键面板无裁切；文件/终端/预览与 Harness 使用相同工作目录；软件 Key 不进入子进程 |
| V0.5 | 第 2 周 | 建立安全和恢复能力 | 自动 Checkpoint；代码/会话 Rewind；会话继续、分支；权限规则；越界写入确认；项目规则与记忆 | 任何 Agent 文件编辑都有恢复点；高风险操作可解释、可拒绝 |
| V0.6 | 第 3 周 | 支持并行和扩展 | 并行会话；Git worktree 隔离；Tasks/Subagents 面板；Side Chat；Skills、Plugins、Hooks、MCP 管理；内置固定版本、隔离运行的 pnpm 插件安装能力 | 用户无需安装 Node.js/pnpm；并行任务不串工作目录；扩展来源、权限、版本、启停状态和回滚点可见 |
| V0.7 | 第 3 周 | 形成受控试点版本 | Git 状态/分支/提交；PR 状态入口；托盘通知；安装版/便携版；备份恢复；诊断；自动更新回滚 | P0=0；安装、升级、覆盖安装、卸载和 packaged smoke 通过 |
| V1.0 | 第 3 周末 | 个人稳定版 | 修复 P0/P1；稳定本地编码主闭环；版本说明、支持边界和回滚预案 | 个人真实仓库连续使用通过；核心异常状态可恢复 |

## 7. 各版本详细能力

### V0.2：Harness 桌面底座

用户能够：

- 双击应用直接进入 DeepSeek Harness。
- 新建、恢复和管理 Harness 会话。
- 配置模型、权限、工具和插件。
- 查看 Harness 启动状态，失败时重试或打开日志。

工程能力：

- 固定 Harness tag/commit/package 和 Node 版本。
- 捆绑平台 Node 与 Harness runtime。
- `HarnessSupervisor` 管理启动、就绪、超时、日志、异常退出和进程树停止。
- 主窗口只加载可信随机 loopback origin。
- 正式路径移除自建 `chat:complete` 和 Renderer 会话实现。

### V0.3：Agent 编程 MVP

用户能够：

- 使用 Windows 原生目录选择打开本地仓库，并从最近仓库快速切换。
- 直接在软件模型设置中维护 DeepSeek API Key，软件托管值不被 Windows 环境变量覆盖。
- 按仓库查看会话历史，继续、重命名和归档会话。
- 在 Plan 模式下只读分析，在批准后进入执行。
- 查看 Agent 正在读取、搜索、编辑、运行命令或测试什么。
- 随时停止 Agent，补充要求后继续。
- 按文件查看 Diff，并接受或拒绝修改。
- 查看测试命令、退出码和失败摘要。

关键门禁：

- 文件未批准前不能伪装成已完成。
- 会话切换后工具状态、Diff 和工作目录不能串线。
- 失败、取消、权限拒绝和网络中断都有可恢复路径。

### V0.4：桌面工作台

当前进度：V0.4.8 已完成图片/PDF 专用预览、命令面板、80%–140% 缩放、完整布局重置和 1024×720 紧凑适配；V0.5 已转入安全恢复能力。

用户能够：

- 自由调整 Chat、Diff、File、Terminal、Preview、Plan 和 Tasks 面板。
- 从对话或 Diff 跳转到具体文件和代码位置。
- 在与 Harness 相同的工作目录中使用一个持久 PowerShell PTY，连续命令、交互提示、ANSI 输出、尺寸同步和页面重载恢复已通过验证。
- 预览 HTML、本地开发服务器、图片和 PDF。
- 使用命令面板打开会话、切换视图、运行测试和管理布局。
- 保存每个项目的面板布局、终端高度和最近视图。

交互要求：

- 键盘优先，完整焦点路径和快捷键帮助。
- 支持 1024×720、1440×900、1920×1080 与最大化窗口。
- 支持浅色、深色、高对比度、文本缩放和减少动态效果。

### V0.5：安全、Checkpoint 与记忆

当前进度：V0.5.0 已发布回合前自动代码检查点；V0.5.1 已发布最近点安全恢复；V0.5.2 已发布最近 12 个检查点的有界历史与选择恢复；V0.5.3 已发布软件内 HTTP(S) 代理和受限剪贴板写入修复；V0.5.4 已完成检查点与 Harness 完整回合关联、代码/会话双操作及官方子会话分支验证。

接下来按小版本收敛：

1. V0.5.3（已完成）：软件内提供直连、Windows 系统代理和自定义 HTTP(S) 代理；保存前可测试 DeepSeek 连通性；修复 Harness 主页面复制按钮，同时继续拒绝剪贴板读取、子框架和其他网页权限。
2. V0.5.4（已完成）：关联 Harness 会话/完整回合与代码检查点；“只恢复代码”和“从该回合建立新会话分支”为两个明确动作，官方 fork 已验证不改写原会话、代码、HEAD 或 Git 索引。
3. V0.5.5：把 Ask、允许、拒绝和越界确认规则集中为可查看、可解释、可撤销的权限界面，继续复用 Harness 权限语义；同时形成独立品牌和无损迁移方案，但不在用户确认名称前改动 V0.5.4 Stable。
4. V0.5.6：展示项目规则、Harness 记忆与本轮上下文来源，支持关闭或编辑用户可控来源，不制造隐形提示。
5. V0.5.7：对 V0.5 恢复链和跨环境网络链做稳定化验收，使用接近 12 个真实检查点的仓库复测性能、损坏 ref、失败回滚、代理异常和安装覆盖；同步验证插件 Profile 一致性检查、固定 pnpm store 和故障插件恢复方案，再决定是否进入 V0.6 并行与扩展能力。

用户能够：

- 在每轮 Agent 编辑前获得自动 Checkpoint。
- 恢复代码，或从检查点关联的完整回合建立新会话分支；两个动作明确分离。
- 从当前会话建立分支，保留原会话、代码和 Git 索引不变。
- 查看并设置 Ask、允许和拒绝规则。
- 查看项目规则、Harness 记忆和当前加载的上下文来源。
- 对越界写入、危险命令、删除和外部操作明确确认。

边界：

- Checkpoint 不替代 Git。
- Bash 或外部进程产生的文件修改若无法完整回退，界面必须明确提示。
- 项目记忆必须可查看、编辑和关闭，不能成为不可见的提示注入。

### V0.6：并行 Agent 与扩展

用户能够：

- 同时启动多个任务。
- 为修改型任务自动创建 Git worktree，实现代码隔离。
- 在 Tasks 面板查看主 Agent、Subagents 和后台命令。
- 向运行中的 Agent 发送补充消息或停止任务。
- 打开 Side Chat 进行临时追问，不污染主会话。
- 浏览、启停和配置 Skills、Plugins、Hooks 与 MCP。
- 查看扩展来源、作用域、权限和加载错误。
- 在原生插件管理器中安装、升级、卸载和回滚可选插件，无需自行安装 Node.js 或 pnpm。
- 查看插件来源、版本、依赖、权限、更新状态和最近一次可用版本。

关键门禁：

- 并行任务不共享可写工作目录。
- 删除 worktree 前展示未提交修改和分支状态。
- 插件或 MCP 失败不能拖垮核心会话。
- 桌面版固定并内置 pnpm 版本，仅在用户数据目录的隔离插件环境中运行，不修改系统级 Node.js/pnpm。
- 界面只开放安装、升级、卸载、启停和回滚等受控操作，不把任意 pnpm 命令执行能力暴露给渲染页面。
- 安装或升级前展示来源、版本和权限；第三方插件必须由用户明确确认，默认不传递软件 Key 或其他凭据。
- 使用锁文件和 last-known-good 快照；安装、升级或启动失败时保留当前可用插件环境并允许一键回滚。
- 插件市场入口必须晚于 Profile manifest/lock/patch 一致性检查、pnpm store 固定、残留目录清理和故障插件识别/卸载；不把社区插件全集静默打进安装包。

### V0.7：试点与开发交付

用户能够：

- 查看 Git 分支、状态、变更和提交入口。
- 从会话进入关联 PR，并查看检查结果和失败链接。
- 通过托盘继续监督后台任务。
- 收到完成、失败和等待确认通知。
- 使用 Windows 安装版或便携版。
- 执行应用数据备份、恢复和脱敏诊断导出。
- 在用户确认后更新桌面、Harness 和插件；检查更新不等于下载，用户接受后才下载，可跳过指定 Latest，失败自动回滚 Stable/last-known-good。

试点要求：

- 用真实但可公开的代码仓库完成理解、修改、测试、Diff、回退和并行任务。
- 安装版 smoke 必须真正启动 Harness 并完成最小编码会话。
- Windows 10/11 x64 安装、覆盖升级、卸载和恢复通过。

### V1.0：稳定发布

具备：

- 稳定的本地 Agent 编程闭环。
- 稳定的项目、会话、计划、工具过程、Diff、终端、预览和恢复体验。
- 稳定的并行 worktree、Subagents 和扩展管理。
- 明确的权限、安全和数据边界。
- 正式安装包、版本说明、已知限制、上游版本映射、许可证清单和回滚预案。

发布门槛：

- P0=0、P1=0。
- 核心主流程和关键异常状态回归通过。
- 更新失败可恢复到 last-known-good。
- API Key、代码正文和项目路径不会进入未授权日志或诊断包。

## 8. 1.0 之后的版本

### V1.1：远程与自动化

- SSH 和远程开发环境。
- 云端或远程后台会话。
- PR 检查监控、失败修复建议和受控合并。
- 定时任务与周期性代码审查。
- GitHub、Slack、Linear 等连接器。

### V1.2：跨设备与 Computer Use

- 在明确授权下控制桌面应用。
- 手机或 Web 端发起、查看和继续任务。
- 跨设备会话迁移和通知。
- 企业集中策略、SSO 和部署控制。

### V2.0：Codex 全场景生产力对标

- Excel、Word、PPT、PDF 和 HTML 的创建、编辑、渲染及交付验证。
- 联网研究、时效信息核验、来源引用和受控浏览器操作。
- Windows 与 Office 操作、可逆安全策略和操作后验证。
- 跨会话项目记忆、长期任务、定时任务、暂停和恢复。
- 多 Agent、线程/项目管理、Skills、Plugins、MCP 和连接器统一路由。
- 以用户现有典型任务原样迁移成功作为验收，而不是以功能入口数量验收。

目标节奏：第 6–10 周形成个人 Beta，第 12–16 周形成稳定版；模型推理质量仍取决于所选 DeepSeek 模型和工具栈。

## 9. 1.0 前明确不做

- ERP、BI、财务或其他行业专用工作台。
- 自建另一套 Agent loop、模型协议、会话和插件体系。
- 云同步、多人实时协作和组织权限中心。
- PR 自动合并、Computer Use 和移动端派发。
- 完整的公开插件市场和视觉皮肤市场（V0.6 只提供有来源、权限确认和回滚能力的可控插件管理器）。
- 为展示“AI 感”增加 KPI 卡片、假进度和无关动画。
- 复制 Claude 品牌、配色、图标或专属商业逻辑。

## 10. 里程碑判定

- **V0.2 底座成立**：桌面应用真正运行 Harness，不再直接调用模型构造另一套聊天系统。
- **V0.3 产品成立**：本地仓库可以完成修改、测试、Diff 和确认闭环。
- **V0.5 可放心试错**：Agent 改动可恢复，权限和记忆可见可控。
- **V0.6 可扩展并行**：多任务、worktree、Subagents 和扩展体系可用。
- **V0.7 可试点**：安装、升级、异常恢复、Git 和后台监督达到真实使用要求。
- **V1.0 可发布**：本地 Claude Code 对标主闭环稳定，P0/P1 清零。

本计划的第一原则是：**以 Claude Code 的 Agent 编程闭环为产品标杆，以 DeepSeek Harness 为实现底座。**
