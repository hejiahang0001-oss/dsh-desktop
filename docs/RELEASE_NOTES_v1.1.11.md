# DSH Desktop V1.1.11 — Latest / Pre-release

固定 DeepSeek Harness `0.1.5-alpha.1`，适配 Session V3 和官方右侧文件面板。本机覆盖安装、安装态及真实模型验收已完成；四项资产已公开并通过独立运行器匿名完整下载校验。595 项测试通过，Stable 保持 V1.1.0。

- 使用官方右侧目录树、多标签文本查看、分屏和聊天文件打开路径；移除桌面重复目录展开，桌面入口调整为文件搜索与只读预览。
- 修复官方文件面板在窄窗口自动全屏后，被桌面审查面板遮挡关闭/还原按钮的问题；仅适配宿主边界，不改官方组件。
- 使用官方发送按钮与 Enter 的排队/插话一致性修复；保留软件代理、最高优先级加密 Key、旧附件引用和草稿。
- 会话交接使用 Inbox 公开待处理列表；历史读取仍走冷读接口，不自动激活 Agent。Wiki 不将新增系统消息导出为用户问题或分析结论。
- 官方 native system 替代 fs-ext，删除对应桌面编译补丁；八项依赖的精确安全修复与固定锁单独记录。
- Office Skills、严格校验、真实产物/备份回执、Wiki 和 Git Review 保留。官方桌面壳迁移及 Portable 优化不在本版。

Session V3 迁移保留旧代文件，但旧程序不能直接继续使用升级后的活动资料目录。必须保留独立升级前备份。安装器仍未签名，不宣称可信自动安装、另一台电脑验收或长期老化已完成。

验证事实见 [VALIDATION](VALIDATION.md)，适配说明见 [上游映射](HARNESS_UPSTREAM_v0.1.5-alpha.1.md)。
