# DSH Desktop 执行进度

> 日期：2026-08-24
> 当前构建：V0.4.4 Daily Build
> 状态：持久交互式 PowerShell PTY 已完成，已直接覆盖本机旧版并通过安装一致性、数据保留和真实 Windows 终端验证

## 本轮完成

1. 将底部终端从单命令运行器升级为同一工作区内持续存在的 PowerShell PTY，可连续输入命令并保留 Shell 状态、交互提示和 ANSI 输出。
2. 使用独立 Node 宿主加载 `node-pty 1.2.0-beta.15`，避免 Electron ABI 与原生模块重建耦合；安装包携带固定 Windows x64 ConPTY 运行资源。
3. 使用 `@xterm/xterm 6.0.0` 与 `@xterm/addon-fit 0.11.0` 提供终端渲染、可访问输入和随面板尺寸调整。
4. Harness 页面重载后可恢复最近 200,000 个字符的终端输出、运行状态、工作目录和尺寸；终端进程本身由主进程持有，不随 Renderer 重载退出。
5. 启动 PTY 前进行一次 Windows 原生风险确认；停止时结束完整进程树，终端运行期间禁用 Git 接受/拒绝，停止后重新建立用户修改保护基线。
6. 软件内维护的 `DEEPSEEK_API_KEY` 不进入 PTY 宿主或 PowerShell 环境；OSC 52 剪贴板写入序列在传给页面前被过滤。
7. 底部面板继续支持快捷键、开关、高度持久化、正常窗口和最大化布局；状态、工作目录、Key 隔离和恢复边界在界面中可见。
8. 自动化回归增至 78 项；真实 Windows PTY 已验证连续命令、Key 隔离、输出恢复、尺寸变更、长任务进程树停止和打包运行资源。
9. 解包版与正式安装版均完成版本、Windows 安全存储、随机回环 HTTP 200、标题和 Workspace 同步烟测。
10. V0.4.4 已直接覆盖安装；原有 13 个会话、软件 Key、桌面状态和工作台布局均保留。

## 验证证据

| 验证项 | 结果 |
|---|---|
| 单元/集成测试 | 78/78 通过 |
| 真实 Windows PTY | 同一 Shell 连续命令通过；软件 Key 不可见；长任务完整进程树停止通过 |
| Renderer 恢复 | 运行状态、工作目录、尺寸和最近 200,000 字符输出可恢复 |
| 终端安全 | 启动前原生确认；`DEEPSEEK_API_KEY` 从宿主和 Shell 环境移除；OSC 52 被过滤 |
| 正式安装版 | V0.4.4、`zh-CN`、`safeStorage=true`；Harness `0.1.1-rc.2`；随机回环 HTTP 200；Workspace 同步成功 |
| 会话持久化 | 覆盖安装前后均为 13 个会话 |
| 用户状态 | 软件 Key、桌面状态与工作台状态哈希在覆盖前后保持一致；未读取或输出 Key 明文 |
| 安装包 | `DSH-Desktop-Setup-0.4.4.exe`；162,555,535 字节 |
| 安装包 SHA-256 | `348E26C211C19344FE59F0FF425F932306F4781953F3EDC11A91F8F4F706FFDC` |
| 直接覆盖 | 安装器退出码 0；Windows 注册项 `DSH Desktop 0.4.4` |
| 安装一致性 | 安装版与解包版 `app.asar` SHA-256 均为 `28C43536BC0C58EA73E10302D42349FB11ACC25237F535B98B5B682E7C9A4757` |
| PTY 宿主 | 安装版与构建版 SHA-256 均为 `E53CCA015B9DBBD8F8702725AE03AD292617196497E27C2EE131C683748C351E` |
| 安装闭包 | 解包版 29,368 个文件；安装版 29,369 个文件；0 个重解析点；0 个终端 PDB |

## 安装与回滚

- 当前正式目录：`%LOCALAPPDATA%\Programs\DSH Desktop`
- V0.4.4 覆盖前快照：`backups/pre-v0.4.4-20260824-001402`
- 快照包含旧程序和非凭据用户数据，共 32,917 个文件、802,333,250 字节。
- 快照中 `.credentials.yaml` 为 0 个；软件 Key 仍只保留在用户数据原位置。
- V0.4.3 和 V0.4.2 前的可用快照继续保留，不覆盖历史回滚点。

## 未宣称完成

- 当前只有一个持久 PTY；尚无终端标签页、分屏、Shell 配置选择或终端会话快照。
- 文件查看仍是有界只读文本预览，不是代码编辑器；没有保存、多标签编辑和语法服务。
- 尚未包含 HTML/本地服务器/图片/PDF 应用预览和端口生命周期管理。
- 尚无自动 Checkpoint、会话级 Rewind 和完整 Plan 取消/恢复状态视图。
- 搜索不读取文件内容，也未实现完整 `.gitignore` 语义。
- rc.8 曾出现 Harness Workspace Write `0xC0000005`；本轮没有用计费模型重测该路径，因此不宣称 rc.2 已修复。
- 软件 Key 仍使用 Harness `0.1.1-rc.2` 的 `.credentials.yaml` 与 Windows 用户目录 ACL，尚未接入 Credential Manager/DPAPI。
- 自动更新、代码签名和 Harness 兼容矩阵流水线尚未完成。
- Windows 界面自动化按安全规则没有向终端输入命令；交互、Key 隔离与进程树停止由真实原生集成测试验证。

## 下一优先级

1. V0.4.5 增加应用预览：先完成 HTML/本地开发服务器与端口生命周期，再扩展图片/PDF。
2. V0.4.6 增加命令面板、快捷键帮助和布局恢复入口。
3. 补齐 1024×720、高对比度、文本缩放和完整键盘焦点视觉矩阵。
4. 下一 Daily Build 继续直接覆盖 V0.4.4，并保留不含凭据的 last-known-good 快照。
5. V0.6 内置固定版本、隔离运行的 pnpm，由原生插件管理器代办安装、升级、卸载、启停和回滚。
