# DSH Desktop 执行进度

> 日期：2026-08-22
> 当前构建：V0.4.0 Daily Build
> 状态：已直接覆盖 V0.3.8；常驻右侧 Git 变更审查面板通过真实仓库、安装版和持久化验证

## 本轮完成

1. 在官方 Harness 页面右侧注入常驻“变更审查”面板，不重建 Agent loop，也不替换官方会话与工具界面。
2. 面板同时显示待审、受保护和已接受文件，可选择文件查看真实工作区 Diff 或暂存区 Diff。
3. 复用既有原生安全门禁完成逐文件/批量接受和拒绝；取消确认不会改变 Git，确认接受只暂存选中文件。
4. 对 Diff 读取建立边界：输出最多 50,000 字符；未跟踪文本预览最多 256 KiB；二进制文件只报告类型。
5. 面板支持 280–520px 拖动、键盘调节、关闭、刷新和重新聚焦；宽度与开关状态写入独立工作台状态文件。
6. 新增 `Ctrl+Alt+D` 显示/隐藏面板、`Ctrl+Alt+J` 聚焦面板，以及原生“视图”菜单中的宽度重置入口。
7. 使用隔离真实 Git 仓库验证待审、保护、接受三种状态；接受后从工作区 Diff 切换为暂存 Diff。
8. 验证 340→418px 拖动持久化、关闭/恢复、最大化布局和 Harness 页面重载后的自动恢复。
9. 自动化回归增至 56 项；全部通过，语法检查和 `git diff --check` 通过。
10. 构建 V0.4.0，在不含凭据的快照后直接覆盖安装；正式安装版哈希、注册版本、Harness smoke、会话、Key 和界面检查通过。

## 验证证据

| 验证项 | 结果 |
|---|---|
| 单元/集成测试 | 56/56 通过 |
| 真实 Git 审查 | 同时识别 1 个待审、2 个受保护文件；接受后正确显示 1 个已接受文件 |
| 真实 Diff | 接受前显示工作区 Diff，接受后显示 `git diff --cached` 暂存 Diff |
| 原生确认 | 默认取消不改变 Git；确认后只暂存 `agent-output.js` |
| 布局持久化 | 面板从 340px 拖至 418px；关闭、快捷键恢复和 Harness 重载后仍保持 |
| 开发态视觉 | 正常窗口与 2560×1440 最大化均无面板裁切 |
| 正式安装版 | 版本 0.4.0；随机回环端口 HTTP 200；标题 `DeepSeek Harness`；Workspace 同步成功 |
| 安装版视觉 | 1208×794 窗口中 Harness 与 340px 右侧审查面板正常显示，空仓库状态真实 |
| 软件 Key | 状态 `configured`、来源 `managed-file`、策略 `software-first`；环境变量被忽略；未输出明文 |
| 会话持久化 | 覆盖安装后 9 个 zstd 持久会话可识别 |
| 安装包 | 158,441,823 字节；7-Zip 检查 `Everything is Ok`；29,278 个文件 |
| 安装包 SHA-256 | `2CB32FAF22F8D76E1CEF30D29209D5B87A50A119EACA26E6262703C3A5DE14E0` |
| V0.3.8 → V0.4.0 覆盖 | 安装器退出码 0；注册版本 `DSH Desktop 0.4.0` |
| 安装一致性 | 正式与解包版 `app.asar` SHA-256 均为 `4BF5251ACD2BCDB92D26A4392DECCB6561789F529F1FA0B2CFCBEB540CEF2690` |
| Harness 文件闭包 | 29,201 个文件，0 个重解析点 |

## 安装与回滚

- 当前正式目录：`%LOCALAPPDATA%\Programs\DSH Desktop`
- V0.4.0 前可用快照：`backups/pre-v0.4.0-20260822-120413`
- 快照包含旧程序、非凭据用户数据和 V0.3.8 安装包，共 31,582 个文件。
- 快照中 `.credentials.yaml` 为 0 个、重解析点为 0 个。
- 第一次备份因旧版窗口占用 Chromium Cookies 而中断，已更名为 `backups/incomplete-pre-v0.4.0-20260822-120253`，它不是可用回滚点。
- 隔离验证仓库与隔离用户数据保留在忽略目录 `artifacts/v0.4.0-panel-e2e-repo` 和 `artifacts/v0.4.0-visual-user-data`。

## 未宣称完成

- V0.4.0 是桌面工作台第一切片，尚未包含文件树、集成终端、应用预览和命令面板。
- 紧凑窗口已有覆盖式布局规则，但尚未完成与正常/最大化窗口同等级别的视觉矩阵验证。
- 尚无自动 Checkpoint、会话级 Rewind 和完整的 Plan 取消/恢复状态视图。
- Workspace Write 下 `pwsh` 仍可能以 `3221225477 (0xC0000005)` 失败；应用准确报告失败，不自动切换 Full Access。
- 软件 Key 使用 Harness rc.8 的 `.credentials.yaml`；Windows 下依赖用户目录 ACL，尚未接入 Credential Manager/DPAPI。
- 自动更新、代码签名和 Harness 兼容矩阵流水线尚未完成。

## 下一优先级

1. 继续 V0.4，增加与 Agent 同工作目录的集成终端，并保持 Harness 为 Agent loop 的唯一事实源。
2. 增加文件树和从 Diff 跳转文件/位置的路径，避免建立第二套会话或模型状态。
3. 补齐 1024×720 紧凑布局和键盘焦点矩阵验证。
4. 完善 Plan 取消、失败、恢复和权限拒绝路径。
5. 下一 Daily Build 继续直接覆盖 V0.4.0，并保留不含凭据的 last-known-good 快照。
