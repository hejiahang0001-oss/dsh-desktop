# DSH Desktop 执行进度

> 日期：2026-08-21  
> 当前构建：V0.3.5 Daily Build  
> 状态：已直接覆盖 V0.3.4；PowerShell 兼容诊断和真实读改测成功路径已通过

## 本轮完成

1. 新增 Windows 原生“工具”菜单，显示最近工具状态、工具类别、测试结果及当前窗口调用/失败/停止计数。
2. 新增“定位当前/最近工具”和“打开工具轨迹”，复用官方 Harness UI，不实现第二套工具执行器。
3. 从对话及轨迹 DOM 提取读取、搜索、编辑、命令、测试等工具类别，并识别运行中、完成、失败和停止状态。
4. 测试结果支持通过、失败、运行中、停止和退出码；兼容 Windows 无符号进程退出码。
5. 增加长轨迹回归：同时保留工具命令头部与退出码尾部，避免长内容截断导致误判。
6. 自动化测试增至 28 项，新增真实 rc.8 轨迹结构、长命令、退出码及折叠工具卡兼容识别回归。
7. 使用软件中已保存的 Key 完成真实模型工具调用；Key 明文未被读取、展示或复制。
8. 定位受限令牌与本机 PowerShell 注入组件的 `0xC0000005` 兼容冲突，不把失败伪装成成功，也不自动切换 Full Access。
9. 使用软件 Key 在隔离 Full Access 会话中完成真实读取、修改和 `pwsh` 测试，1/1 通过、退出码 0，并经独立测试复核。
10. 构建 V0.3.5，完整备份 V0.3.4 后直接覆盖安装；安装版与构建版一致，Harness HTTP 200。

## 验证证据

| 验证项 | 结果 |
|---|---|
| 单元/集成测试 | 28/28 通过 |
| 软件 Key | 已配置；覆盖安装后保持可用；未复制进备份 |
| 真实模型与工具调用 | `DeepSeek-V4-Flash High` 成功发起 1 次 `pwsh` 工具调用 |
| 工具进程结果 | rc.8 Workspace Write 下异常退出 `3221225477 (0xC0000005)` 且无输出；不宣称测试通过 |
| 工具状态菜单 | “最近失败”“命令”“测试失败（退出码 3221225477）”；`1 次调用 · 1 次失败 · 0 次停止` |
| 轨迹定位 | 原生入口打开官方轨迹，真实 `TOOL pwsh` 行和退出码可见 |
| 开发态/安装版 Harness | 随机回环端口 HTTP 200，标题 `DeepSeek Harness` |
| 隔离成功路径 | Full Access 会话真实读取、修改并运行一次 `pwsh` 测试；1/1 通过，退出码 0 |
| 安装包 | 158,426,165 字节 |
| 安装包 SHA-256 | `DC35B5A083E9D2788DFF381A3F0898AD0BF132FCC996B258075C26FAF1C0D369` |
| V0.3.4 → V0.3.5 最终覆盖 | 安装器退出码 0 |
| 安装一致性 | 注册表版本 0.3.5；正式与解包版 `app.asar` SHA-256 均为 `CB9B91C6A47FC4038BC3AF98819A309FBEA25E16C059F304DCB03CD95B82DBF9` |
| 会话持久化 | 当前 5 个持久会话文件可见，覆盖前会话未丢失 |

## 安装与回滚

- 当前正式目录：`%LOCALAPPDATA%\Programs\DSH Desktop`
- V0.3.5 前完整备份：`backups/pre-v0.3.5-20260821-190126`
- 备份包含 V0.3.4 程序、非凭据用户数据和旧安装包；`.credentials.yaml` 明确排除。
- 备份中凭据文件 0 个、重解析点 0 个。

## 未宣称完成

- V0.3 的工具/测试失败可见性和隔离成功读改测路径已经完成，但 Workspace Write 的 PowerShell 兼容问题及 Diff 接受/拒绝闭环尚未完成。
- “继续”当前复用 Harness 的会话和输入机制，尚未形成独立的桌面恢复/继续状态视图。
- 软件 Key 使用 Harness rc.8 的 `.credentials.yaml`；Windows 下依赖用户目录 ACL，尚未接入 Credential Manager/DPAPI。
- 自动更新、代码签名和 Harness 兼容矩阵流水线尚未完成。

## 下一优先级

1. 接入文件 Diff，并提供接受、拒绝和回退的最小闭环。
2. 验证真实文件任务的权限确认、取消和恢复路径。
3. 建立 Harness 版本兼容矩阵，持续跟踪受限 PowerShell 执行问题。
4. 补齐成功、失败、停止三类工具任务的安装版回归样本。
5. 下一 Daily Build 继续直接覆盖 V0.3.5，并保留不含凭据的 last-known-good 备份。
