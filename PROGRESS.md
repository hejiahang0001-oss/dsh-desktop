# DSH Desktop 执行进度

> 日期：2026-08-23
> 当前构建：V0.4.3 Daily Build
> 状态：已直接覆盖本机旧运行时；官方 Harness `0.1.1-rc.2`、中文过程层、真实模型和安装一致性验证完成

## 本轮完成

1. 将固定离线运行时从官方 `@deepseek-ai/dsh@0.1.0-rc.8` 跟随升级为 `0.1.1-rc.2`，带入视觉模型、图像上传复用/预处理、多行提问输入和上游安全修复。
2. 新增 DSH Desktop Harness 配置补丁；中文提问时，可见思考、工具调用说明、计划、任务、进度、问题和最终答复默认全部使用简体中文。
3. 用户明确要求其他语言时继续遵从；代码、命令、路径、标识符、API 名称、错误原文和原始工具输出保持原样。
4. 在 Harness 页面注入有界翻译观察器，修正上游硬编码的 `Session log`、`Think`、`Thinking` 和 `(no output)`。
5. 翻译观察器跳过 `code`、`pre`、`kbd`、`samp`、输入框和可编辑区域，不改写可复制的技术内容。
6. 历史会话中已经生成的英文过程继续保留；从 V0.4.3 起的新模型回合按语言规则生成，不篡改会话日志。
7. 中文提示词补丁独立进入安装资源；缺失时 Harness 明确失败并提示重装，避免静默丢失汉化规则。
8. 完成插件三层审计：官方源码 234 个包清单、安装闭包 188 个 `dsh`/`dsh-*` 包、默认 Web 组合 135 个插件行；社区插件和非默认替代包未静默内置。
9. 自动化回归增至 74 项；提示词配置合成、启动参数、缺失补丁失败路径、固定标签和代码区保护均已覆盖。
10. 解包版与正式安装版均完成随机回环 HTTP 200、标题、Workspace 同步、版本和安装一致性烟测。
11. 使用软件内已有 Key 发起真实 `DeepSeek-V4-Flash` 中文回合；2 秒返回预期中文结果，证明 Key、模型请求、会话写入和语言补丁链路真实可用。

## 验证证据

| 验证项 | 结果 |
|---|---|
| 单元/集成测试 | 74/74 通过 |
| Harness 版本 | 官方 `@deepseek-ai/dsh@0.1.1-rc.2`；npm 发布版本、CLI 版本与打包闭包一致 |
| 插件边界 | 188 个打包 `dsh`/`dsh-*` 包；官方默认 Web 配置 135 个插件行；社区插件未内置 |
| 提示词配置 | Harness `--dump-config` 可见完整语言规则；中文覆盖可见思考、工具说明、计划、进度、问题和结论 |
| 界面固定标签 | 安装包携带翻译观察器，覆盖 `Session log`、`Think`、`Thinking`、`(no output)`；代码和编辑区跳过 |
| 正式安装版 | V0.4.3、`zh-CN`、`safeStorage=true`；内置 Harness `0.1.1-rc.2`；随机回环 HTTP 200；Workspace 同步成功 |
| 软件 Key | `configured`、`managed-file`、`software-first`；环境变量被忽略；未输出明文 |
| 真实模型 | `DeepSeek-V4-Flash High`；1 轮、1 步；2 秒完成；中文响应符合测试指令 |
| 会话持久化 | 覆盖安装后原 12 个 zstd 会话可识别；真实验证新增 1 个会话后为 13 个 |
| 安装包 | 158,566,564 字节；7-Zip `Everything is Ok`；29,311 个文件 |
| 安装包 SHA-256 | `039460E59FBCDE18F70C724926E473AC74FE02CFE06EEA5C47CAD3E77D5B181E` |
| 直接覆盖 | 安装器退出码 0；注册项 `DSH Desktop 0.4.3`；用户数据与软件 Key 保留 |
| 安装一致性 | 安装版与解包版 `app.asar` SHA-256 均为 `C37A4B871DC3D8CD999AECF632982E222F350A972E6F25A1943FFD5A0E38C70A` |
| 中文补丁一致性 | 源码、解包版和安装版 SHA-256 均为 `4C3E3ED042A45635055BBDD0A41432212495F686404EE878DB985B7AEBCE3A68` |
| 安装闭包 | 29,312 个文件，其中 Harness 29,233 个文件；0 个重解析点 |

## 安装与回滚

- 当前正式目录：`%LOCALAPPDATA%\Programs\DSH Desktop`
- 本次 Harness rc.2 覆盖前快照：`backups/pre-v0.4.3-rc2-20260823-171542`
- 快照包含旧 V0.4.3 程序和非凭据用户数据，共 32,560 个文件、779,888,842 字节。
- 快照中 `.credentials.yaml` 为 0 个、重解析点为 0 个。
- V0.4.2 前可用快照继续保留在 `backups/pre-v0.4.2-20260822-143830`。

## 未宣称完成

- 文件查看是有界只读文本预览，不是代码编辑器；没有语法高亮、行级跳转、保存或多标签编辑。
- 当前终端仍是受控单命令运行器，不是持久交互式 PTY；不支持交互提示、终端标签页、Shell 会话状态和完整 ANSI 渲染。
- 搜索不读取文件内容，也未实现完整 `.gitignore` 语义；当前只固定跳过 `.git`、`node_modules` 和链接。
- 尚未包含 HTML/本地服务器/图片/PDF 应用预览和命令面板。
- 尚无自动 Checkpoint、会话级 Rewind 和完整 Plan 取消/恢复状态视图。
- rc.8 曾出现 Harness Workspace Write `0xC0000005`；本轮未用写文件任务复现或宣称 rc.2 已修复，集成终端仍不自动提升 Harness 权限。
- 软件 Key 仍使用 Harness `0.1.1-rc.2` 的 `.credentials.yaml` 与 Windows 用户目录 ACL，尚未接入 Credential Manager/DPAPI。
- 自动更新、代码签名和 Harness 兼容矩阵流水线尚未完成。
- 本轮按用户授权使用软件 Key 发起了一次真实计费模型请求；未读取、复制或输出 Key 明文。
- 历史会话里已持久化的英文思考/工具说明不会被追溯翻译；固定标签会在页面渲染时显示中文。

## 下一优先级

1. V0.4.4 进行持久交互式 PTY 技术切片：交互、进程树、权限、恢复和 Key 隔离未全部通过前不替换当前终端。
2. V0.4.5 增加应用预览（先 HTML/本地开发服务器，再图片/PDF）与端口生命周期管理。
3. V0.4.6 增加命令面板、快捷键帮助和布局恢复入口。
4. 补齐 1024×720、最大化、高对比度、文本缩放和完整键盘焦点视觉矩阵。
5. 下一 Daily Build 继续直接覆盖 V0.4.3，并保留不含凭据的 last-known-good 快照。
6. V0.6 内置固定版本、隔离运行的 pnpm，由原生插件管理器代办安装、升级、卸载、启停和回滚；用户无需另装 Node.js/pnpm。
