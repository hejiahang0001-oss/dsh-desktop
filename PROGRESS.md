# DSH Desktop 执行进度

> 日期：2026-08-21  
> 当前构建：V0.3.6 Daily Build
> 状态：已直接覆盖 V0.3.5；Diff 定位和安全接受/拒绝最小能力已交付

## 本轮完成

1. 接入 rc.8 官方 `[data-diff]` 和 Produced 文件入口，可从 Windows 原生“变更”菜单定位最近 Diff。
2. 新增最近产物状态：待审查、原有修改保护、已接受并暂存、已恢复、仅可查看。
3. “接受并暂存”通过固定参数调用 Git，将文件当前内容写入暂存区，不自动提交或推送。
4. 已跟踪文件“拒绝并恢复”回到当前 Git 暂存版本；未跟踪新文件进入 Windows 回收站。
5. Agent 运行或等待确认时禁用接受/拒绝；非 Git 工作区保持只读 Diff 定位。
6. 打开仓库和进入新 Agent 回合时捕获已有未暂存/未跟踪路径，命中原有修改时禁止一键拒绝。
7. 自动化测试增至 36 项，真实临时 Git 仓库覆盖暂存、后续恢复、新文件、路径越界和原有修改保护。
8. 开发版与安装版均实际启动，原生“变更”菜单的布局、状态、快捷键和禁用态通过视觉检查。
9. 构建 V0.3.6，完成 V0.3.5 非凭据快照后直接覆盖；安装器退出码 0，安装版 Harness HTTP 200。
10. 使用软件 Key 做真实单文件复验时发现 rc.8 内部“DSH工作区”未与桌面活动路径自动对齐；任务已主动停止，临时文件未被修改，测试对象已移入可恢复归档。

## 验证证据

| 验证项 | 结果 |
|---|---|
| 单元/集成测试 | 36/36 通过 |
| Git 接受/拒绝 | 真实临时仓库验证暂存、恢复、新文件和原有修改保护通过 |
| 开发版/安装版菜单 | “变更”菜单、`Ctrl+Shift+D`、状态说明和禁用态可见 |
| 解包版 Harness | 随机回环端口 HTTP 200，版本 0.3.6，标题 `DeepSeek Harness` |
| 正式安装版 Harness | 随机回环端口 HTTP 200，版本 0.3.6，标题 `DeepSeek Harness` |
| 软件 Key | 覆盖安装后仍为“已配置、软件优先”；未读取、显示或复制明文 |
| 会话持久化 | 覆盖前 5 个会话全部保留；真实复验新增 1 个测试会话 |
| 安装包 | 158,429,726 字节；7-Zip 检查 `Everything is Ok` |
| 安装包 SHA-256 | `40C68DF82BD7090AEC7903CE5EE723612323462FC2E4E6B699AB9F6F307F616B` |
| V0.3.5 → V0.3.6 覆盖 | 安装器退出码 0；注册表版本 0.3.6 |
| 安装一致性 | 正式与解包版 `app.asar` SHA-256 均为 `459A2E09353AB938CEFE764431C65274A9394F1B08B7C120C91F96B0AC7D6181` |
| Harness 文件闭包 | 29,201 个文件，0 个重解析点；NSIS 介质含 29,278 个文件 |

## 安装与回滚

- 当前正式目录：`%LOCALAPPDATA%\Programs\DSH Desktop`
- V0.3.6 前完整快照：`backups/pre-v0.3.6-20260821-211911`
- 快照包含 V0.3.5 程序、非凭据用户数据、5 个会话和旧安装包。
- 快照共 59,815 个文件（含清单），重解析点 0 个，`.credentials.yaml` 0 个。
- 真实复验临时 Git 对象已从用户数据目录移入 `backups/v0.3.6-e2e-temp`，可恢复；`launch-root` 已恢复为空。

## 未宣称完成

- 当前是“最近可见产物文件”的最小审查能力，不是独立多文件 Diff 面板；尚无批量接受/拒绝、Checkpoint 或 Rewind。
- 安装版真实复验确认桌面活动路径和 rc.8 客户端内部“DSH工作区”不会自动对齐，因此尚未形成真实仓库端到端 Diff 确认通过证据。
- Workspace Write 下 `pwsh` 仍会以 `3221225477 (0xC0000005)` 失败；应用准确报告失败，不自动切换 Full Access。
- 软件 Key 使用 Harness rc.8 的 `.credentials.yaml`；Windows 下依赖用户目录 ACL，尚未接入 Credential Manager/DPAPI。
- 自动更新、代码签名和 Harness 兼容矩阵流水线尚未完成。

## 下一优先级

1. 让 Windows 原生“打开代码仓库”与 rc.8 客户端工作区使用同一根目录，并完成真实模型读改 Diff 复验。
2. 把最近单文件审查扩展为多文件 Diff 列表和逐文件接受/拒绝。
3. 验证真实文件任务的权限确认、取消和恢复路径。
4. 建立 Harness 版本兼容矩阵，持续跟踪 Workspace Write PowerShell 问题。
5. 下一 Daily Build 继续直接覆盖 V0.3.6，并保留不含凭据的 last-known-good 快照。
