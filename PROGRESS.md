# DSH Desktop 执行进度

> 日期：2026-08-22
> 当前构建：V0.4.2 Daily Build
> 状态：已直接覆盖 V0.4.1；工作区文件树、只读文件查看、Diff 定位和安装版验证完成

## 本轮完成

1. 在 Harness 左侧增加与当前 Workspace 同步的文件面板；根目录和子目录按需读取，不在启动时扫描整个仓库。
2. 文件名搜索限制为最多 80 个结果、2,000 个目录、20,000 个条目、16 层和 1.5 秒；结果显示相对路径以区分同名文件。
3. 安全文本使用只读 Quick Look 浮层查看，显示相对路径、类型、编码、行数和大小；`Esc` 关闭并恢复原焦点。
4. 文本预览上限为 512 KiB，仅支持 UTF-8 与 UTF-16 LE；二进制、链接、特殊文件和不支持编码不会读取。
5. `.env*`、凭据/secret 命名、常见私钥名和 `.pem/.key/.pfx/.p12` 默认不可预览；面板不跟随符号链接或目录联接。
6. 所有文件 IPC 只接受当前可信随机回环 Harness 页面，并再次执行相对路径、真实路径和工作区包含关系校验。
7. 右侧 Diff 增加“查看文件”；真实验证可自动清空搜索、展开父目录、选中文件并打开预览。
8. 左侧文件面板支持 220–380px 拖动/键盘调节、隐藏、聚焦和宽度重置；`Ctrl+Alt+E` 显示/隐藏，`Ctrl+Alt+F` 聚焦搜索。
9. 工作台状态升级到 schema v3，继续保留审查面板和终端状态，并新增文件面板开关与宽度。
10. 三面板布局已在真实仓库验证：左侧文件、中间 Harness、右侧 Diff、底部终端互不冒充彼此的状态源。
11. 自动化回归增至 70 项；真实文件树、搜索、预览、Esc、Diff 定位和安装版视觉均通过。
12. 构建 V0.4.2，在无凭据快照后直接覆盖 V0.4.1；注册版本、安装一致性、Harness smoke、软件 Key 和会话检查通过。

## 验证证据

| 验证项 | 结果 |
|---|---|
| 单元/集成测试 | 70/70 通过 |
| 文件路径安全 | 拒绝绝对路径、父目录跳转、空段、符号链接和目录联接 |
| 文件内容安全 | 凭据/私钥、二进制、大于 512 KiB、非 UTF-8/UTF-16 LE 文本不预览 |
| 文件搜索边界 | 最多 80 结果、2,000 目录、20,000 条目、16 层、1.5 秒；跳过 `.git`、`node_modules` 和链接 |
| 真实视觉 | 懒加载树、相对路径搜索、只读预览、Esc、Diff→文件定位和三面板布局通过 |
| 正式安装版 | 版本 0.4.2；随机回环端口 HTTP 200；标题 `DeepSeek Harness`；Workspace 同步成功 |
| 软件 Key | `configured`、`managed-file`、`software-first`；环境变量被忽略；未输出明文 |
| 会话持久化 | 覆盖安装后 10 个 zstd 会话可识别 |
| 安装包 | 158,454,677 字节；7-Zip `Everything is Ok`；29,278 个文件 |
| 安装包 SHA-256 | `90375B47B566619DDBB119A99839DFB28798B7EA52F460ECE0EEF83349EAFC53` |
| V0.4.1 → V0.4.2 覆盖 | 安装器退出码 0；注册版本 `DSH Desktop 0.4.2` |
| 安装一致性 | 安装版与解包版 `app.asar` SHA-256 均为 `CBCD2A07483D340FC7907FC0B9468FC10753A3EA1901E0F8925FD4A8D7076AE3` |
| Harness 文件闭包 | 29,201 个文件，0 个重解析点 |

## 安装与回滚

- 当前正式目录：`%LOCALAPPDATA%\Programs\DSH Desktop`
- V0.4.2 前快照：`backups/pre-v0.4.2-20260822-143830`
- 快照包含 V0.4.1 程序、非凭据用户数据和 V0.4.1 安装包，共 32,234 个文件、919,802,566 字节。
- 快照中 `.credentials.yaml` 为 0 个、重解析点为 0 个。
- V0.4.1 前可用快照继续保留在 `backups/pre-v0.4.1-20260822-133000`。

## 未宣称完成

- 文件查看是有界只读文本预览，不是代码编辑器；没有语法高亮、行级跳转、保存或多标签编辑。
- 当前终端仍是受控单命令运行器，不是持久交互式 PTY；不支持交互提示、终端标签页、Shell 会话状态和完整 ANSI 渲染。
- 搜索不读取文件内容，也未实现完整 `.gitignore` 语义；当前只固定跳过 `.git`、`node_modules` 和链接。
- 尚未包含 HTML/本地服务器/图片/PDF 应用预览和命令面板。
- 尚无自动 Checkpoint、会话级 Rewind 和完整 Plan 取消/恢复状态视图。
- Harness Workspace Write 的 `0xC0000005` 上游风险仍存在；集成终端不自动提升 Harness 权限。
- 软件 Key 仍使用 Harness rc.8 的 `.credentials.yaml` 与 Windows 用户目录 ACL，尚未接入 Credential Manager/DPAPI。
- 自动更新、代码签名和 Harness 兼容矩阵流水线尚未完成。

## 下一优先级

1. V0.4.3 进行持久交互式 PTY 技术切片：交互、进程树、权限、恢复和 Key 隔离未全部通过前不替换当前终端。
2. V0.4.4 增加应用预览（先 HTML/本地开发服务器，再图片/PDF）与端口生命周期管理。
3. V0.4.5 增加命令面板、快捷键帮助和布局恢复入口。
4. 补齐 1024×720、最大化、高对比度、文本缩放和完整键盘焦点视觉矩阵。
5. 下一 Daily Build 继续直接覆盖 V0.4.2，并保留不含凭据的 last-known-good 快照。
